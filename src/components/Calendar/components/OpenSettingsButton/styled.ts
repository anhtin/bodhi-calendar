import styled from 'styled-components';

export const Button = styled.button`
  position: fixed;
  bottom: 1vh;
  align-self: flex-end;
  border: none;
  background: none;
  cursor: pointer;
  font-size: 4rem;
`;
