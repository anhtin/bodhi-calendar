import styled from 'styled-components';

export const Button = styled.button`
  position: absolute;
  bottom: 1vh;
  align-self: flex-end;
  border: none;
  background: none;
  cursor: pointer;
  font-size: 4rem;
  z-index: 1;
`;
